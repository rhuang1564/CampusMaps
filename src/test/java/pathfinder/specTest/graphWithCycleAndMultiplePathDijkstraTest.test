# Find a path with Dijkstra's algorithm
# with a cycle in the graph and multiple paths

CreateGraph graph

AddNode graph A
AddNode graph B
AddNode graph C
AddNode graph D

AddEdge graph C D 10.0
AddEdge graph A B 1.0
AddEdge graph B C 1.0
AddEdge graph D A 1.0
AddEdge graph C A 1.0

FindPath graph A D
