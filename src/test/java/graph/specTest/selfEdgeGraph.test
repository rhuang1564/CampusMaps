# Tests adding a single node to the graph with a self edge

# Create a graph
CreateGraph A

# Add a node
AddNode A node1

# Add an edge
AddEdge A node1 node1 edge1

# Print the nodes in the graph and checks its edges
ListNodes A
ListChildren A node1